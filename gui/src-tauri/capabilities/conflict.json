{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "conflict",
  "description": "独立冲突解决窗口的权限。allow-destroy 必需:窗口注册了 onCloseRequested,Tauri 在回调未 preventDefault 时内部调 destroy() 收尾,缺这条权限则红叉与 close() 都关不掉窗口。",
  "windows": ["conflict"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-focus",
    "dialog:allow-ask",
    "dialog:allow-message"
  ]
}
