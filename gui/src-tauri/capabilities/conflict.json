{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "conflict",
  "description": "Capability for the standalone conflict-resolution window",
  "windows": ["conflict"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "dialog:allow-ask",
    "dialog:allow-message"
  ]
}
